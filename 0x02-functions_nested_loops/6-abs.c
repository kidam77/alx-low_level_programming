#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: the integer to be operated upon
 *
 * Return: an unassigned int value to calling function
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
